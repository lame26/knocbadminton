# Copy this file to .env.local and set only for production API.
# Leave blank during local dev to use Vite proxy (/api -> http://localhost:8787).

# VITE_API_URL=https://knoc-badminton-api.your-subdomain.workers.dev
