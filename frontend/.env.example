# 이 파일을 .env.local 로 복사하고 실제 값을 입력하세요
# .env.local 은 절대 git에 커밋하지 마세요!

# Workers API URL (프로덕션 배포 후 실제 Workers 도메인으로 변경)
# 개발 중에는 vite.config.ts의 proxy 덕분에 이 값 없이도 동작합니다
VITE_API_URL=https://knoc-badminton-api.your-subdomain.workers.dev
